"use strict";

/* global */

$(document).ready(() => {
    //...
    
    $(window).on("resize", "", (event) => {
        //...
    });
    
    $(window).on("orientationchange", "", (event) => {
        //...
    });
});